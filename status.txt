================================================================================
                      COVERT CHATTER
              COMPLETE PROJECT STATUS & FULL CHRONOLOGICAL RECORD
================================================================================
Last Updated: 2026-08-16 19:30:00 IST
Repository Location: e:\Covert Chat
Git Branch: main
Latest Git Commit: Pass 7 Complete (Rebranding to Covert Chatter & Unified 1-Screen Pairing Flow)
Overall System Health: 100% OPERATIONAL & VERIFIED (All Passes Complete)

================================================================================
1. PROJECT PURPOSE, MISSION & CORE SECURITY MODEL
================================================================================
Covert Chatter (Whisper Drop) is a zero-knowledge, ultra-private, ephemeral
WebRTC communication platform designed for direct peer-to-peer (P2P) messaging
and secure data exchange without any intermediary server storage.

Foundational Security & Cryptographic Principles:
1. Zero Knowledge:
   - Central servers NEVER receive, inspect, log, or persist any message text,
     encryption keys, files, or user metadata.
2. Hardware & Sandboxed Keystore Isolation (Pass 1):
   - Cryptographic keypairs (ECDSA P-256 for signing, ECDH P-256 for key agreement)
     are generated with `extractable: false`.
   - Private key bytes remain locked inside the browser's cryptographic module
     and cannot be extracted via JavaScript or malicious XSS scripts.
3. Direct Peer-to-Peer Data Channels & Relays (Pass 2 & Pass 6):
   - Communications flow directly between browser instances over WebRTC `RTCDataChannel` ("chat").
   - Negotiated via Google STUN (`stun:stun.l.google.com:19302`) and optional TURN servers (`turn:...`).
   - Even when passing through TURN relays on restrictive symmetric NATs, the relay only forwards
     encrypted DTLS/SCTP ciphertext — zero plaintext exposure.
4. Authenticated End-to-End Encryption - E2EE (Pass 3):
   - Ephemeral AES-256-GCM symmetric session keys derived via ECDH Diffie-Hellman.
   - Fresh, cryptographically secure 12-byte (96-bit) IVs generated for every single message.
   - Every message ciphertext is digitally signed with the sender's non-extractable ECDSA private key.
   - Recipients cryptographically verify signatures before attempting decryption (silent drop on tamper).
5. True Ephemeral Message Lifecycle & Anti-Persistence (Pass 4):
   - Expiry timestamps and burn parameters are cryptographically signed inside the inner payload.
   - Default TTL: 60 seconds ticking down in real time.
   - Burn-on-Read: 5-second self-destruct countdown initiated upon recipient render.
   - Zero Persistence Invariant: Messages are held ONLY in transient React state (volatile RAM).
     Never written to localStorage, sessionStorage, or IndexedDB.
   - Memory is immediately purged on room exit, tab close, or 30-second background blur.
6. Encrypted Streaming File Drops (Pass 5):
   - 16KB chunked streaming over WebRTC DataChannel.
   - Chunk-level AES-256-GCM encryption with fresh IVs + ECDSA signatures.
   - SCTP backpressure control (`bufferedAmountLowThreshold` 32KB / 64KB high watermark).
   - In-memory `Blob` URLs auto-revoked (`URL.revokeObjectURL`) upon expiry or room teardown.

================================================================================
2. LIVE ACTIVE RUNTIME SERVICES & ENVIRONMENT
================================================================================
* Backend Signaling Server:
  - Status: ACTIVE & RUNNING (Background Daemon, Task ID: task-18)
  - Endpoint: ws://localhost:8080 (also proxied via /ws on client)
  - Process: node server.js
  - Process Working Directory: e:\Covert Chat\server
  - Role: Relays WebRTC session descriptions (SDP) and ICE candidates between peers; zero storage.

* Frontend Client Application:
  - Status: ACTIVE & RUNNING (Vite Dev Server, Task ID: task-151)
  - Local URL: http://localhost:5173
  - Network URL: http://192.168.31.25:5173/
  - Single-Tunnel WebSocket Proxy: Path `/ws` forwards automatically to `http://localhost:8080` (ws: true)
  - Allowed Remote Hosts: `.ngrok-free.dev`, `.ngrok-free.app`
  - Dynamic Signaling Resolution: `getDefaultSignalingUrl()` auto-derives `ws://` / `wss://` from window.location
  - Framework: React 19.0.0 + Vite 6.1.0 (Module Bundler)
  - Working Directory: e:\Covert Chat\client

* Build & Bundling Health:
  - Production Build: PASSING (`npm run build`)
  - Modules Transformed: 1,845 modules
  - Bundle Size: index.html (1.17 kB), index.css (13.22 kB), index.js (659.08 kB)
  - Compile Time: ~4.35s with 0 errors.

================================================================================
3. SYSTEM ARCHITECTURE & CRYPTOGRAPHIC TOPOLOGY
================================================================================

 [ Browser Peer A ] <==== WebRTC P2P DataChannel ("chat") ====> [ Browser Peer B ]
   - Persistent ID: IndexedDB                                     - Persistent ID: IndexedDB
   - Signing: ECDSA NIST P-256                                    - Signing: ECDSA NIST P-256
   - Agreement: ECDH NIST P-256                                   - Agreement: ECDH NIST P-256
   - Fingerprint: SHA-256 Digest                                  - Fingerprint: SHA-256 Digest
   - Derived Session Key: AES-256-GCM (ECDH Derived)              - Derived Session Key: AES-256-GCM (ECDH Derived)
   - Connection Quality: Direct (STUN) or Relayed (TURN)          - Connection Quality: Direct (STUN) or Relayed (TURN)
   - Ephemeral Memory Store: Transient React RAM Only             - Ephemeral Memory Store: Transient React RAM Only
   - Inner Payload: { id, text, sentAt, expiresAt, burnOnRead }   - Inner Payload: { id, text, sentAt, expiresAt, burnOnRead }
   - Wire Envelope: { iv, ciphertext, signature, id, timestamp }  - Wire Envelope: { iv, ciphertext, signature, id, timestamp }
   - RTCPeerConnection (STUN + Configured TURN Relays)            - RTCPeerConnection (STUN + Configured TURN Relays)
   - Inbound ICE Queue Buffer                                     - Inbound ICE Queue Buffer
         \                                                                     /
          \---- (WebSocket Signaling Relay /ws -> ws://localhost:8080) -------/
                                         |
                                         v
                             [ Node.js Signaling Server ]
                              - Port: 8080 (Proxied at /ws)
                              - In-Memory Maps (rooms, clientRooms)
                              - Zero Logs / Zero Persistence

================================================================================
4. COMPONENT & CODEBASE SUBSYSTEM BREAKDOWN
================================================================================

--------------------------------------------------------------------------------
[A] BACKEND SIGNALING SERVER (server/)
--------------------------------------------------------------------------------
* Location: e:\Covert Chat\server
* Entry Point: server.js (3.8 KB)
* Package Config: package.json (whisper-drop-server v1.0.0, dependency: ws@^8.18.0)
* Documentation: README.md

--------------------------------------------------------------------------------
[B] FRONTEND CORE BUILD & STACK (client/)
--------------------------------------------------------------------------------
* Location: e:\Covert Chat\client
* Stack: React 19.0.0, Vite 6.1.0, Lucide React 1.16.0, QRCode.react 4.2.0, html5-qrcode 2.3.8
* Vite Config: `client/vite.config.js`
  - Single-tunnel WebSocket proxy (`/ws` -> `http://localhost:8080` with `ws: true`)
  - Allowed remote hosts for ngrok (`allowedHosts: ['.ngrok-free.dev', '.ngrok-free.app']`)
* Environment Configuration:
  - `client/.env.example`: Safe open-source template for `VITE_TURN_URL`, `VITE_TURN_USERNAME`, `VITE_TURN_CREDENTIAL`.
  - `client/.env`: Local gitignored credentials.

--------------------------------------------------------------------------------
[C] CRYPTOGRAPHIC CORE ENGINE (client/src/utils/crypto.js)
--------------------------------------------------------------------------------
* Location: e:\Covert Chat\client\src\utils\crypto.js
* Functions:
  - `generateUserIdentity()`: Generates ECDSA P-256 + ECDH P-256 keypairs (`extractable: false`).
  - `computeFingerprint()`: Computes 64-char hex SHA-256 digest of concatenated public keys.
  - `exportPublicJWK()`: Exports only public JWKs for peer exchange.
  - `deriveSharedSecret()`: Derives 256-bit AES-GCM session key from local private ECDH key + remote public ECDH JWK.
  - `validatePeerIdentityPayload()`: Strictly validates schema and base64url keys of peer identity inputs.

--------------------------------------------------------------------------------
[D] MESSAGE ENCRYPTION & SIGNING LAYER (client/src/utils/encryption.js)
--------------------------------------------------------------------------------
* Location: e:\Covert Chat\client\src\utils\encryption.js
* Functions:
  - `encryptMessage()`: Encrypts UTF-8 plaintext with AES-256-GCM using fresh 12-byte IV.
  - `decryptMessage()`: Decrypts AES-256-GCM ciphertext using derived session key.
  - `signMessage()`: Signs base64 ciphertext with sender's ECDSA P-256 private key (IEEE P1363).
  - `verifyMessage()`: Verifies digital signature against sender's ECDSA public key.

--------------------------------------------------------------------------------
[E] PERSISTENT IDENTITY STORAGE LAYER (client/src/utils/db.js)
--------------------------------------------------------------------------------
* Location: e:\Covert Chat\client\src\utils\db.js
* Database Name: "whisper_drop_identity" (IndexedDB)
* Features: Structured Clone `CryptoKey` handles storage, browser compatibility fallback error messaging, `resetIdentity()` manual wipe.
* Invariant: Strictly decoupled from ephemeral chat messages — zero messages are ever written to IndexedDB.

--------------------------------------------------------------------------------
[F] WEBRTC & ENCRYPTED EPHEMERAL SERVICE (client/src/utils/webrtc.js)
--------------------------------------------------------------------------------
* Location: e:\Covert Chat\client\src\utils\webrtc.js
* Class: `PeerSession`
* Features:
  - Dynamic signaling URL resolution: `getDefaultSignalingUrl()` resolves origin-relative `ws://` / `wss://.../ws`.
  - Configured ICE servers via `getIceServers()` (Google STUN + TURN credentials via `import.meta.env`).
  - Receiving-side ICE candidate queuing buffer (buffers candidates before `setRemoteDescription` completes).
  - 15-second connection timeout guard ("Connection failed — please check your network or try again").
  - `getConnectionType()`: Inspects `RTCPeerConnection.getStats()` active candidate pair (`direct` vs `relay`).
  - Automatic AES-256-GCM session key derivation on DataChannel open.
  - Full encryption pipeline on `sendMessage(text)` and streaming `sendFile(file)`.

--------------------------------------------------------------------------------
[G] USER INTERFACE COMPONENTS (client/src/components/)
--------------------------------------------------------------------------------
* `ConnectRoom.jsx`:
  - Ephemeral chat box with live countdown badges (`⏳ 58s` / `🔥 Burn in 4s`).
  - Active 1-second interval garbage collection removing expired messages from React state.
  - "Burn on read (5s)" toggle for instant post-view self-destruction.
  - Connection Quality Indicator Badge: "Direct connection" (⚡ host/srflx) vs "Relayed connection" (🖥️ TURN relay).
  - Drag-and-drop file staging zone & 16KB encrypted streaming progress bars.
  - 30-second background blur auto-purge and tab close wipe.
  - Dynamic Signaling URL input with one-click reset to origin default.
* `MyIdentity.jsx`: High-density `QRCodeSVG`, raw payload/fingerprint copy, SVG download, identity regeneration with confirmation modal.
* `QRScanner.jsx`: Live webcam QR scanner via `html5-qrcode` with emerald viewfinder frame, file image fallback, and "Use in Connect Room" action.
* `KeyDetails.jsx`: Raw JWK viewers and educational security notes on non-extractable keys.
* `Toast.jsx`: Glassmorphic floating toast notification component.

--------------------------------------------------------------------------------
[H] APP SHELL & STYLING (client/src/App.jsx, client/src/index.css)
--------------------------------------------------------------------------------
* `App.jsx`: Top navigation across Connect, My Identity, Scan Peer, Key Details; shared `verifiedPeer` state; status pills with active fingerprints.
* `index.css`: Dark glassmorphic design system with emerald (`#10b981`), cyan (`#06b6d4`), and flame amber (`#f59e0b`) accents.

================================================================================
5. WEBSOCKET SIGNALING PROTOCOL SPECIFICATION MATRIX
================================================================================
| Direction       | Message Type        | Payload Structure                                               | Action / State Transition                                      |
|-----------------|---------------------|-----------------------------------------------------------------|----------------------------------------------------------------|
| Client -> Server| `create`            | `{"type": "create"}`                                            | Requests new 6-char room code                                  |
| Server -> Client| `created`           | `{"type": "created", "room": "XXXXXX"}`                         | Returns room code; sets host to "Waiting for peer"             |
| Client -> Server| `join`              | `{"type": "join", "room": "XXXXXX"}`                            | Joiner requests to enter room                                  |
| Server -> Client| `joined`            | `{"type": "joined", "room": "XXXXXX"}`                          | Joiner confirmed in room                                       |
| Server -> Host  | `ready`             | `{"type": "ready"}`                                             | Signals host to create DataChannel & generate SDP offer        |
| Peer <-> Peer   | `offer`             | `{"type": "offer", "sdp": ...}`                                 | Relays SDP offer to joiner                                     |
| Peer <-> Peer   | `answer`            | `{"type": "answer", "sdp": ...}`                                | Relays SDP answer to host                                      |
| Peer <-> Peer   | `candidate`         | `{"type": "candidate", "candidate": ...}`                       | Relays ICE candidates (buffered before setRemoteDescription)   |
| Server -> Peer  | `peer-disconnected` | `{"type": "peer-disconnected"}`                                 | Notifies remaining peer when connection drops; room destroyed   |
| Server -> Client| `error`             | `{"type": "error", "message": "Room not found" | "Room is full"}`| Signals signaling error                                        |

================================================================================
6. COMPLETE CHRONOLOGICAL MILESTONES LOG
================================================================================
- [x] Milestone 0: Initialized Git repository, configured `.gitignore`, and set up project documentation.
- [x] Milestone 1: Implemented zero-knowledge WebRTC signaling server in `server/server.js` (`ws` library).
- [x] Milestone 2: Documented signaling protocol and privacy guarantees in `server/README.md`.
- [x] Milestone 3 (Pass 1): Built Client Identity Subsystem:
      * ECDSA P-256 signing keypair + ECDH P-256 agreement keypair (`extractable: false`).
      * SHA-256 public identity fingerprinting.
      * Persistent IndexedDB storage (`whisper_drop_identity`) with browser fallback error handling.
      * High-density QR code generator & live webcam + file image QR scanner.
      * Raw JWK inspection & security architecture documentation.
- [x] Milestone 4 (Pass 2): Built WebRTC Signaling & Direct P2P DataChannel:
      * Implemented `PeerSession` WebRTC manager connected to `ws://localhost:8080`.
      * Added receiving-side ICE candidate queuing buffer before `setRemoteDescription()`.
      * Added 15-second connection timeout guard.
      * Built `ConnectRoom.jsx` UI with room code display, join input, and live chat box.
- [x] Milestone 5 (Pass 3): Built End-to-End Cryptographic Encryption (E2EE):
      * ECDH 256-bit AES-GCM session key derivation.
      * AES-GCM encryption with unique 12-byte IVs for every message.
      * ECDSA digital signature generation and verification.
      * Peer identity binding in ConnectRoom with verified fingerprint badge.
- [x] Milestone 6 (Pass 4): Built Ephemeral Messaging UX & Anti-Persistence Self-Destruction:
      * Signed expiry timestamps embedded in authenticated payload.
      * Default 60-second live ticking countdown timer.
      * 5-second Burn-on-Read trigger activated upon recipient screen render.
      * 1-second memory garbage collection loop in React state.
      * 30-second tab blur & tab close zero-persistence memory purge.
- [x] Milestone 7: Verified production build (`npm run build` passing with 0 errors).
- [x] Milestone 8: Started backend signaling server daemon on `ws://localhost:8080`.
- [x] Milestone 9: Verified zero persistence in DevTools Console (localStorage, sessionStorage, IndexedDB).
- [x] Milestone 10 (Pass 5): Built Encrypted Ephemeral File Sharing Subsystem:
      * 16KB chunking streaming protocol over WebRTC DataChannel (`client/src/utils/fileTransfer.js`).
      * Chunk-level AES-256-GCM encryption with fresh IVs + ECDSA digital signatures on every chunk.
      * WebRTC SCTP Backpressure flow control via `bufferedAmountLowThreshold` (32KB) and `bufferedamountlow` event pausing on 64KB high watermark.
      * Out-of-order chunk index reassembly and total chunk count integrity verification in `FileReceiver`.
      * Configurable file expiry selector (`60s`, `3 min` default, `5 min`, `10 min`) + Burn-on-Read.
      * Drag-and-drop zone overlay and file attachment staging modal in `ConnectRoom.jsx`.
      * Anti-persistence in-memory `Blob` URLs with auto-revocation (`URL.revokeObjectURL`) upon expiry or room disconnect.
      * Verified with clean production build (`npm run build`).
- [x] Milestone 11 (Pass 6): Built TURN Server Integration & Cross-Network Traversal:
      * Integrated TURN server configuration in `client/src/utils/webrtc.js` alongside public STUN servers.
      * Secure credentials loaded via `import.meta.env` (`VITE_TURN_URL`, `VITE_TURN_USERNAME`, `VITE_TURN_CREDENTIAL`).
      * Created `client/.env.example` template for clean open-source configuration.
      * Updated 15-second timeout and ICE connection failure guards to "Connection failed — please check your network or try again".
      * Built active candidate pair type detection via `RTCPeerConnection.getStats()` in `PeerSession.getConnectionType()`.
      * Added connection quality badge in `ConnectRoom.jsx` ("Direct connection" vs "Relayed connection").
      * Cryptographic invariant maintained: TURN server acts strictly as an encrypted byte relay with zero plaintext access (E2EE holds 100%).
- [x] Milestone 13 (Rebrand to Covert Chatter & Unified 1-Screen Pairing Flow):
      * Complete rebranding across UI, HTML meta tags, package names, console logs, and READMEs.
      * Database migration in `client/src/utils/db.js`: updated DB name to `covert_chatter_identity` with automatic one-time migration from `whisper_drop_identity`.
      * Unified 1-screen pairing flow in `ConnectRoom.jsx` with automatic on-scan chaining and session memory reconnect.
- [x] Milestone 14 (Pale, Clean, Minimal Light Theme Redesign):
      * Completely replaced dark cyberpunk theme with a calm, minimal light theme (#FAFAFA canvas, #FFFFFF cards, #E5E7EB borders).
      * Integrated calm sage green / muted teal accent color (#6B9B7C) for actions, reserving red/amber strictly for burn timers and destructive actions.
      * Removed neon glow effects and multi-badge clutter; folded technical encryption labels into a single small "Encrypted" badge with tap-to-expand details modal.
      * Redesigned chat stream with modern Signal / iMessage style light bubbles and clean typography.
      * Simplified message input bar to clean text field, file attachment, burn-on-read toggle, and send button with zero redundant status noise.
      * Mobile-first responsive optimization with generous tap targets and clean camera QR scanning.
      * Verified with clean production build (`npm run build` passing with 0 errors).
- [x] Maintained comprehensive live tracking in `status.txt`.

================================================================================
7. UPCOMING ROADMAP & FUTURE MILESTONES
================================================================================
1. Future Polish:
   - Optional encrypted P2P audio/video stream negotiation.
   - Offline multi-device QR exchange enhancements.
================================================================================
